# README patch contents

This patch replaces the repository root `README.md` and installs a curated set of paper figures under `docs/assets/readme/`.

Key corrections:

- uses GitHub-compatible `$...$` and `$$...$$` mathematical delimiters;
- reorganizes the README into **Approach** and **Results**;
- explains the macrosystem before individual packages;
- maps every equation to a safety box and implementation path;
- includes landing, contingency, alpha-sensitivity, and solver-comparison figures;
- uses the actual environment name `.venv_boxes`;
- distinguishes validated results from claims that still require stronger experiments;
- describes sequential failure as contingency-exhaustion detection, not a validated physical HOLD maneuver.
